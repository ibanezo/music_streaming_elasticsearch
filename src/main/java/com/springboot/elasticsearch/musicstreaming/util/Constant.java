package com.springboot.elasticsearch.musicstreaming.util;

public interface Constant {
    // clusterName = music_app_cluster
    // (indexName = "songs", type = "default", shards = 2)
    String INDEX = "songs";
    String TYPE = "default";
}
